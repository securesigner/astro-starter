/**
 * Vitest Setup for React Testing
 *
 * Extends Vitest matchers with jest-dom for DOM assertions.
 * Import this in vitest.config.ts as setupFiles.
 */

import '@testing-library/jest-dom/vitest';
